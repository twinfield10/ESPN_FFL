[pytest]
testpaths = tests
addopts = -q -m "not live"
markers =
    live: hits the live ESPN API; needs config.yaml credentials and network.
           Deselected by default; run with `pytest -m live`.
